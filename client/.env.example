# URL de la API en producción (Railway). En desarrollo no hace falta:
# el proxy de Vite redirige /api a http://localhost:4000
# Ejemplo: VITE_API_URL=https://cuenta-clara.up.railway.app/api
VITE_API_URL=
